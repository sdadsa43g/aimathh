"""AIMathH — Universal Mathematical & Physics AI Research Harness.

The harness wraps an arbitrary AI model with computational, verification,
simulation, visualization and research infrastructure so that the pipeline is::

    MODEL -> PLAN -> COMPUTE -> VERIFY -> CROSS-CHECK -> CRITIQUE -> PRESENT

instead of ``MODEL -> ANSWER``.
"""

__version__ = "0.1.0"
__all__ = ["__version__"]
